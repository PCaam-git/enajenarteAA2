# Entorno de desarrollo local
# La API se ejecuta desde IntelliJ/Maven y MariaDB desde Docker

server.port=8080

spring.datasource.url=jdbc:mariadb://localhost:3307/${MARIADB_DATABASE}
spring.datasource.username=${MARIADB_USER}
spring.datasource.password=${MARIADB_PASSWORD}